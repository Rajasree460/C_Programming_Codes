//*there are three intvalues ,which one is maximum8//
#include<stdio.h>
int main()
{
	int a,b,c,max;
	printf("enter three integer values\n");
	scanf("%d%d%d",&a,&b,&c);
	if (a>b)
	if (a>c)
	  max a;
	 else
	  max c;
	 else
	  if(b>c)
	   max b;
	  else
	   max c;
	 printf("maximum is %d",max);
	 return 0;     
}
